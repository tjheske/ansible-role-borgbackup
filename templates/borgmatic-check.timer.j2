[Unit]
Description=Run borgmatic backup checks

[Timer]
OnCalendar=*-*-{{ borgmatic_cron_checks_day }} {{ borgmatic_cron_checks_hour }}:{{ borgmatic_cron_checks_minute }}
Persistent=true

[Install]
WantedBy=timers.target
